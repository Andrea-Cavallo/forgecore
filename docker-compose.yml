version: "3.9"

networks:
  forgecore:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  minio_data:
  vault_data:

services:

  # ── Infrastructure ────────────────────────────────────────────────────────────

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-forgecore}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      POSTGRES_DB: ${POSTGRES_DB:-forgecore}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks: [forgecore]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-forgecore}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    command: redis-server --save "" --appendonly no
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks: [forgecore]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  nats:
    image: nats:2.10-alpine
    command: ["-js", "-c", "/etc/nats/nats.conf"]
    volumes:
      - ./deployments/nats/nats.conf:/etc/nats/nats.conf:ro
    ports:
      - "4222:4222"
      - "8222:8222"
    networks: [forgecore]
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    networks: [forgecore]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

  vault:
    image: hashicorp/vault:1.17
    command: server -dev -dev-root-token-id=root
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: root
      VAULT_ADDR: http://0.0.0.0:8200
    volumes:
      - vault_data:/vault/data
    ports:
      - "8200:8200"
    networks: [forgecore]
    cap_add: [IPC_LOCK]

  jaeger:
    image: jaegertracing/all-in-one:1.57
    ports:
      - "16686:16686"
      - "4317:4317"
      - "4318:4318"
    networks: [forgecore]

  prometheus:
    image: prom/prometheus:v2.51.0
    volumes:
      - ./deployments/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    networks: [forgecore]

  grafana:
    image: grafana/grafana:10.4.0
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - ./deployments/grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3000:3000"
    networks: [forgecore]
    depends_on: [prometheus]

  # ── Microservices ─────────────────────────────────────────────────────────────

  forgecore-auth:
    build:
      context: .
      dockerfile: services/forgecore-auth/Dockerfile
    env_file: .env
    ports:
      - "${AUTH_PORT:-8081}:8081"
      - "${AUTH_GRPC_PORT:-9091}:9091"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}
      redis: {condition: service_healthy}

  forgecore-payments:
    build:
      context: .
      dockerfile: services/forgecore-payments/Dockerfile
    env_file: .env
    ports:
      - "${PAYMENT_PORT:-8082}:8082"
      - "${PAYMENT_GRPC_PORT:-9092}:9092"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}
      nats: {condition: service_healthy}

  forgecore-notifications:
    build:
      context: .
      dockerfile: services/forgecore-notifications/Dockerfile
    env_file: .env
    ports:
      - "${NOTIFICATION_PORT:-8083}:8083"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}
      nats: {condition: service_healthy}

  forgecore-admin:
    build:
      context: .
      dockerfile: services/forgecore-admin/Dockerfile
    env_file: .env
    ports:
      - "${ADMIN_PORT:-8084}:8084"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}

  forgecore-audit:
    build:
      context: .
      dockerfile: services/forgecore-audit/Dockerfile
    env_file: .env
    ports:
      - "${AUDIT_PORT:-8085}:8085"
      - "${AUDIT_GRPC_PORT:-9095}:9095"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}
      nats: {condition: service_healthy}

  forgecore-jobs:
    build:
      context: .
      dockerfile: services/forgecore-jobs/Dockerfile
    env_file: .env
    networks: [forgecore]
    depends_on:
      redis: {condition: service_healthy}
      nats: {condition: service_healthy}

  forgecore-gateway:
    build:
      context: .
      dockerfile: services/forgecore-gateway/Dockerfile
    env_file: .env
    ports:
      - "${GATEWAY_PORT:-8080}:8080"
    networks: [forgecore]
    depends_on:
      - forgecore-auth

  forgecore-permissions:
    build:
      context: .
      dockerfile: services/forgecore-permissions/Dockerfile
    env_file: .env
    ports:
      - "${PERMISSION_PORT:-8087}:8087"
      - "${PERMISSION_GRPC_PORT:-9097}:9097"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}

  forgecore-config:
    build:
      context: .
      dockerfile: services/forgecore-config/Dockerfile
    env_file: .env
    ports:
      - "${CONFIG_PORT:-8088}:8088"
      - "${CONFIG_GRPC_PORT:-9098}:9098"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}
      redis: {condition: service_healthy}

  forgecore-webhooks:
    build:
      context: .
      dockerfile: services/forgecore-webhooks/Dockerfile
    env_file: .env
    ports:
      - "${WEBHOOK_PORT:-8089}:8089"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}
      nats: {condition: service_healthy}

  forgecore-storage:
    build:
      context: .
      dockerfile: services/forgecore-storage/Dockerfile
    env_file: .env
    ports:
      - "${STORAGE_PORT:-8090}:8090"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}
      minio: {condition: service_healthy}

  forgecore-subscriptions:
    build:
      context: .
      dockerfile: services/forgecore-subscriptions/Dockerfile
    env_file: .env
    ports:
      - "${SUBSCRIPTION_PORT:-8091}:8091"
      - "${SUBSCRIPTION_GRPC_PORT:-9099}:9099"
    networks: [forgecore]
    depends_on:
      postgres: {condition: service_healthy}
